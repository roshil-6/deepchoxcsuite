/** Shared executive / AI Studio surface — use across cards and panels */
export const executiveSurfaceClass =
    'bg-[var(--surface)] border border-[var(--border)] rounded-2xl';

export const executiveCardPadding = 'p-5';

export const executiveCardClass = `${executiveSurfaceClass} ${executiveCardPadding}`;
